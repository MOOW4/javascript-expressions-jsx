import React from "react";
import ReactDOM from "react-dom";

const name = "Christian";

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<h1>Hello, Mom! <br></br>It's me {name}</h1>);